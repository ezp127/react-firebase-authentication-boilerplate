## React + Firebase
###  Authentication boilerplate

<br>

Instructions:

1. Copy `.env.example` to `.env.local`. 
2. Fill it up with your [Firebase](https://firebase.google.com) credentials. 
3. See `package.json` for available scripts.